# Copia este archivo como .env y reemplaza los valores con tus propias credenciales.
# No subas tu archivo .env real al repositorio (ya esta ignorado en .gitignore).

MONGO_URI=mongodb+srv://<usuario>:<password>@<cluster>.mongodb.net/<basededatos>?retryWrites=true&w=majority
PORT=3000
